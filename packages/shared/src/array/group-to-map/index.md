## `groupToMap`

```ts
// Arrow function
groupToMap(arr, (element) => { /* … */ })
groupToMap(arr, (element, index) => { /* … */ })
groupToMap(arr, (element, index, array) => { /* … */ })

// Callback function
groupToMap(arr, callbackFn)
groupToMap(arr, callbackFn, thisArg)

// Inline callback function
groupToMap(arr, (element) => { /* … */ })
groupToMap(arr, (element, index) => { /* … */ })
groupToMap(arr, (element, index, array) => { /* … */ })
groupToMap(arr, (element, index, array) => { /* … */ }, thisArg)
```
